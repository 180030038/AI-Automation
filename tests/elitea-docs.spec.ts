import { test, expect } from '@playwright/test';

// Test: Navigate to elitea.ai and open Documentation

test('Navigate to elitea.ai and open Documentation', async ({ page }) => {
  // initial navigation will be performed in the test steps added next
});
